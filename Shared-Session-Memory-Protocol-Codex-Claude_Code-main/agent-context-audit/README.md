# Agent Memory Reset — guidance

Small instructions for resetting local agent memory during development.

1. Clear local private memories for your tool clients (for example, `~/.<tool>/memories`).
2. Rebuild `session-memory/index.json` by scanning `session-memory/receipts/`.
